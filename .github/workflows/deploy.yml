name: Deploy

on:
  push:
    branches: [main, dev]

env:
  PROJECT_ID: arc-ctc-project
  REGION: us-west1
  AR_REPO: arc-reactor
  IMAGE_NAME: arc-reactor
  ORCHESTRATOR_IMAGE_NAME: nextflow-orchestrator

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v4

      - name: Set environment
        id: vars
        run: |
          if [ "${GITHUB_REF_NAME}" = "main" ]; then
            echo "env_name=prod" >> "$GITHUB_OUTPUT"
            echo "service_name=arc-reactor" >> "$GITHUB_OUTPUT"
          else
            echo "env_name=test" >> "$GITHUB_OUTPUT"
            echo "service_name=arc-reactor-dev" >> "$GITHUB_OUTPUT"
          fi

      - name: Auth to GCP
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.GCP_SA_KEY }}

      - name: Set up gcloud
        uses: google-github-actions/setup-gcloud@v2
        with:
          project_id: ${{ env.PROJECT_ID }}

      - name: Configure Docker
        run: gcloud auth configure-docker ${{ env.REGION }}-docker.pkg.dev

      - name: Build and push image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: Dockerfile
          push: true
          tags: ${{ env.REGION }}-docker.pkg.dev/${{ env.PROJECT_ID }}/${{ env.AR_REPO }}/${{ env.IMAGE_NAME }}:${{ github.sha }}
          secrets: |
            GITHUB_TOKEN=${{ secrets.GITHUB_TOKEN }}

      - name: Build and push orchestrator image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: orchestrator/Dockerfile.orchestrator
          push: true
          platforms: linux/amd64
          tags: |
            ${{ env.REGION }}-docker.pkg.dev/${{ env.PROJECT_ID }}/${{ env.AR_REPO }}/${{ env.ORCHESTRATOR_IMAGE_NAME }}:${{ github.sha }}
            ${{ env.REGION }}-docker.pkg.dev/${{ env.PROJECT_ID }}/${{ env.AR_REPO }}/${{ env.ORCHESTRATOR_IMAGE_NAME }}:latest

      - name: Deploy to Cloud Run
        run: |
          gcloud run deploy "${{ steps.vars.outputs.service_name }}" \
            --region "${{ env.REGION }}" \
            --image "${{ env.REGION }}-docker.pkg.dev/${{ env.PROJECT_ID }}/${{ env.AR_REPO }}/${{ env.IMAGE_NAME }}:${{ github.sha }}" \
            --platform managed \
            --service-account "arc-reactor@${{ env.PROJECT_ID }}.iam.gserviceaccount.com" \
            --set-env-vars "DYNACONF=${{ steps.vars.outputs.env_name }},FRONTEND_OUT_DIR=/app/frontend/out" \
            --set-secrets "BENCHLING_PROD_API_KEY=benchling-prod-api-key:latest,BENCHLING_PROD_DATABASE_URI=benchling-prod-database-uri:latest,BENCHLING_PROD_APP_CLIENT_ID=benchling-prod-app-client-id:latest,BENCHLING_PROD_APP_CLIENT_SECRET=benchling-prod-app-client-secret:latest,BENCHLING_TEST_API_KEY=benchling-test-api-key:latest,BENCHLING_TEST_DATABASE_URI=benchling-test-database-uri:latest,BENCHLING_TEST_APP_CLIENT_ID=benchling-test-app-client-id:latest,BENCHLING_TEST_APP_CLIENT_SECRET=benchling-test-app-client-secret:latest,GOOGLE_API_KEY=google-api-key:latest" \
            --no-allow-unauthenticated
